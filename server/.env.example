# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
# For SQLite (zero-config local development):
DATABASE_URL="file:./dev.db"

# For PostgreSQL (production deployment):
# DATABASE_URL="postgresql://postgres:password@localhost:5432/engineering_day_2026?schema=public"

# Authentication Secrets
JWT_SECRET=replace_with_a_secure_random_secret_string_minimum_32_characters
JWT_EXPIRES_IN=7d

# Initial Admin Credentials (used during database seed)
ADMIN_EMAIL=parmjeetyadav1230@gmail.com
ADMIN_PASSWORD="Engineeringday@2026"

# CORS Allowed Origin
CORS_ORIGIN=http://localhost:5173

# Payment Configuration (defaults, configurable in admin UI)
DEFAULT_UPI_ID=engineeringday2026@upi
DEFAULT_UPI_NAME=Engineering Day 2026 Organizers

# Coordinator Contacts
COORDINATOR_PARMJEET="Parmjeet Yadav : 9467843851"
COORDINATOR_PRIYANSHU="Priyanshu Sharma : 7541841303"

# File Upload Settings
MAX_FILE_SIZE_MB=5

# Email SMTP Settings (Optional - mock logger fallback used if empty)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false
EMAIL_FROM="Engineering Day 2026 <no-reply@engineeringday2026.edu>"
